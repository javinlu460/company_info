server {
    listen 80 default_server;
    server_name _;

    # 前台官网（company-web）
    location / {
        root /www/company-web/dist;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 后台管理系统（company-cms），如需单独域名可放到另一个 server 块
    location /admin {
        alias /www/company-cms/dist;
        index index.html;
        try_files $uri $uri/ /admin/index.html;
    }

    # 后台静态资源
    location /admin/assets/ {
        alias /www/company-cms/dist/assets/;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 上传文件访问（后端上传目录）
    location /uploads/ {
        alias /data/company_info/uploads/;
    }
}

# 如需后台使用独立域名 admin.example.com，可取消注释下面这段
# server {
#     listen 80;
#     server_name admin.example.com;
#
#     location / {
#         root /var/www/company-cms;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:8080/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
